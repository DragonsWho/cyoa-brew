cyoa-interactive/
├── index.html                 # Минимальный loader
├── styles/
│   ├── main.css              # Основные стили
│   └── themes.css            # Темы оформления
├── src/
│   ├── core/
│   │   ├── engine.js         # Игровая логика
│   │   ├── state.js          # Управление состоянием
│   │   └── rules.js          # Система правил
│   ├── ui/
│   │   ├── renderer.js       # Отрисовка страниц
│   │   ├── tooltip.js        # Система подсказок
│   │   └── controls.js       # Элементы управления
│   └── utils/
│       ├── coords.js         # Работа с координатами
│       └── evaluator.js      # Безопасный eval
├── config/
│   ├── schema.json           # JSON Schema валидация
│   └── example.json          # Пример конфига
└── docs/
    ├── llm-guide.md          # Руководство для LLM
    └── rule-catalog.md       # Каталог правил




    # CYOA Interactive System - Quick Start Guide

Система для превращения статичных CYOA в интерактивные игры.

## 🎯 Что это делает?

1. Показывает оригинальную картинку CYOA
2. Рисует прозрачные кнопки поверх карточек
3. Считает очки автоматически
4. Проверяет требования и несовместимости

## 📁 Структура файлов

```
cyoa-interactive/
├── README.md                  ← вы здесь
├── index.html                 ← ЗАПУСКАТЬ ЭТОТ ФАЙЛ
├── generate_test_image.html   ← генератор тестовой картинки
├── styles/
│   └── main.css
├── src/
│   ├── main.js
│   ├── core/
│   │   ├── engine.js
│   │   ├── state.js
│   │   └── rules.js
│   ├── ui/
│   │   ├── renderer.js
│   │   ├── tooltip.js
│   │   └── controls.js
│   └── utils/
│       └── coords.js
└── config/
    ├── test_config.json       ← тестовая конфигурация
    └── test_image.png         ← тестовая картинка

```

## 🚀 Как запустить (3 способа)

### Способ 1: Локальный сервер (РЕКОМЕНДУЕТСЯ)

ES6 модули требуют HTTP сервер. Выберите один из вариантов:

#### Python (если установлен):
```bash
# Python 3
python -m http.server 8000

# Python 2
python -m SimpleHTTPServer 8000
```

#### Node.js (если установлен):
```bash
npx http-server -p 8000
```

#### VS Code:
Установите расширение "Live Server" и нажмите "Go Live"

Затем откройте: **http://localhost:8000**

---

### Способ 2: Без сервера (Firefox)

Firefox разрешает модули с `file://`:

1. Откройте Firefox
2. Перетащите `index.html` в окно браузера
3. ✅ Должно работать!

---

### Способ 3: Chrome с флагом (не рекомендуется)

```bash
chrome.exe --allow-file-access-from-files
```

⚠️ Небезопасно для постоянного использования!

---

## 📝 Как создать тестовую картинку

### Вариант А: Автоматически

1. Откройте `generate_test_image.html` в браузере
2. Нажмите "Generate Test Image"
3. Сохраните как `config/test_image.png`

### Вариант Б: Вручную

Создайте любую картинку 1920x1080px с несколькими зонами для кнопок.
Можно в Paint, Photoshop, или онлайн на https://pixlr.com

---

## 🎮 Интерфейс управления

После запуска вы увидите:

| Кнопка | Что делает |
|--------|------------|
| 📖 Translation | Показать текст поверх карточек (для переводов) |
| 🐞 Debug | Показать границы всех зон |
| 🔄 Reset | Сбросить все выбранное |

Внизу экрана - панель с очками.

---

## 🧪 Тестирование системы

### Что должно работать в тестовом примере:

✅ Клик по карточке выделяет её зелёным  
✅ Очки пересчитываются автоматически  
✅ Карточки с требованиями становятся доступны после выбора нужных  
✅ Несовместимые карточки блокируются  
✅ При наведении показывается подсказка  
✅ "Choose 1" группа работает как радио-кнопка  

---

## 📊 Структура конфига

Минимальный рабочий пример:

```json
{
  "meta": {
    "title": "Test CYOA",
    "pages": ["config/test_image.png"]
  },
  "points": [
    {
      "id": "points",
      "name": "Points",
      "start": 10
    }
  ],
  "groups": [
    {
      "id": "test_group",
      "title": "Test Group",
      "page": 0,
      "items": [
        {
          "id": "item1",
          "title": "Free Item",
          "coords": {"x": 100, "y": 100, "w": 300, "h": 200},
          "description": "This item is free!",
          "cost": []
        },
        {
          "id": "item2",
          "title": "Costs 5 Points",
          "coords": {"x": 450, "y": 100, "w": 300, "h": 200},
          "description": "This costs 5 points",
          "cost": [{"currency": "points", "value": -5}]
        }
      ]
    }
  ]
}
```

---

## 🐛 Устранение проблем

### "Failed to load module" ошибка

**Причина:** Браузер блокирует ES6 модули с `file://`

**Решение:** Используйте локальный сервер (см. выше)

---

### Картинка не загружается

**Проверьте:**
1. Путь в `config.json` → `"pages": ["config/test_image.png"]`
2. Файл существует в папке `config/`
3. Имя файла написано правильно (регистр важен!)

**Отладка:**
Откройте консоль браузера (F12) и посмотрите ошибки сети

---

### Кнопки не реагируют

**Откройте Debug Mode** (кнопка 🐞):
- Должны появиться красные рамки вокруг зон
- Если рамок нет - проблема с координатами

**Проверьте координаты:**
```json
{
  "coords": {
    "x": 100,    // от 0 до ширины картинки
    "y": 100,    // от 0 до высоты картинки  
    "w": 300,    // ширина зоны
    "h": 200     // высота зоны
  }
}
```

---

### Очки считаются неправильно

**Проверьте знак `value`:**
- Отрицательное число = трата: `"value": -5`
- Положительное число = получение: `"value": 5`
- Ноль = бесплатно: `"value": 0`

**Откройте консоль (F12)** и ищите ошибки

---

## 📖 Дальнейшее изучение

После успешного запуска тестового примера:

1. **Изучите** `docs/rule-catalog.md` - все доступные правила
2. **Посмотрите** `config/test_config.json` - примеры всех механик
3. **Измените** координаты и посмотрите как двигаются кнопки
4. **Добавьте** свою картинку и создайте конфиг

---

## 🆘 Помощь

**Консоль браузера (F12)** - ваш лучший друг!

Полезные команды в консоли:
```javascript
// Посмотреть текущее состояние
CYOA.engine.state

// Посмотреть конфиг
CYOA.engine.config

// Выбрать карточку программно
CYOA.engine.select('item_id')

// Сбросить всё
CYOA.engine.reset()
```

---

## ✨ Следующие шаги

1. ✅ Запустить тестовый пример
2. ✅ Понять как работают координаты
3. ✅ Изучить config структуру
4. 📚 Прочитать `docs/rule-catalog.md`
5. 🎨 Создать свою первую CYOA
6. 🤖 Использовать LLM для автоматизации

---

**Готово к запуску! 🚀**

Если что-то не работает - смотрите консоль браузера (F12) и ищите красный текст с ошибками.